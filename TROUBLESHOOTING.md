# Troubleshooting

## Agent Timeout
If an agent times out, the system uses cached data or lowers the weight for that agent.

## API Rate Limits
Each service has rate limits. The system queues requests to stay within limits.

## Low Scores
Final score below 30 means avoid. 30-50 means watch. Above 50 is potential buy.
